# Root local dev env (copy to .env)
#
# Shared Sanity project settings used by both apps.
SANITY_PROJECT_ID=
SANITY_DATASET=

# Optional (defaults are handled in code/tooling as needed)
SANITY_API_VERSION=2024-01-01

