/**
 * JS/media-query mirrors for client islands. Must stay aligned with CSS:
 * @see apps/web/src/styles/global.css — primary nav mobile layout (~263–280).
 */
export const NAV_MOBILE_MQ = "(max-width: 47.9375rem)" as const;
